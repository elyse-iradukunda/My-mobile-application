# Base URL of the NestJS backend (it mounts every route under /api).
# Use your machine's LAN IP when running on a real device, e.g. http://192.168.1.10:3000/api
EXPO_PUBLIC_API_URL=http://localhost:3000/api
